{
  "buildCommand": "cd ../.. && yarn build --filter=@stableview/app...",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd ../.. && yarn install",
  "devCommand": "cd ../.. && yarn dev --filter=@stableview/app",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
